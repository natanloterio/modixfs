name: counter
description: Increments a counter on each read.

files:
  - name: increment
    type: read_invoke
    handler: |
      FILE=/tmp/lf_counter
      count=$(cat "$FILE" 2>/dev/null || echo 0)
      echo $((count + 1)) | tee "$FILE"
